#include <stdio.h>
#include "main.h"
/**
 * main - main block
 * @argc: counter
 * @argv: array name
 * Description: prints the number of arguments passed into it
 * Return: 0;
 */
int main(int argc, char *argv[])
{
	printf("%d\n", argc -1);
	return (0);
}
