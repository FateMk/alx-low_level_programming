#include "main.h"
/*
 * _islower - islower block
 * Description: checks if a character is lowercase
 * Return: 1 
 * else Return: 0
 */
int _islower(int c)
{
	char a = 'A';
	/*checks if lowercase*/
	if (a == 'a')
		return (1);
	else
		return (0);
}
